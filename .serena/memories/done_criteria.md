Before finishing work: run relevant checks for the changed area, verify behavior not only syntax, keep error handling explicit, and ensure no new hardcoded secrets, unsafe defaults, dead code, or silent fallbacks are introduced. If schema, route contracts, runtime policy, or repo guidance changed, update the matching docs and Serena memory files in the same pass. If the change touched `.codex/agents/`, `.agents/skills/`, `.serena/`, or other repo-shape metadata Serena relies on, rerun `uvx --from git+https://github.com/oraios/serena serena project index .` and `uvx --from git+https://github.com/oraios/serena serena project health-check .` from the repo root before closing the task.
