TypeScript ESM codebase with relative imports using .js extensions. Use backend/src/utils/logger.ts rather than console logging. Configuration belongs in backend/src/config. Read existing patterns before editing and prefer the smallest correct change. Protect live-trading safety first; validate the full execution path for trading changes. Route provider calls through the shared Helius/Birdeye services so quota accounting, batching, caching, and auth stay centralized. Workers should reuse backend/src/db/client.ts rather than creating ad-hoc Prisma clients. Avoid touching root-level files unless the task requires it.
