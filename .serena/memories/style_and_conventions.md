TypeScript ESM codebase with relative imports using .js extensions. Use backend/src/utils/logger.ts rather than console logging. Configuration belongs in backend/src/config. Read existing patterns before editing and prefer the smallest correct change. Protect live-trading safety first; validate the full execution path for trading changes. Avoid touching root-level files unless the task requires it.
