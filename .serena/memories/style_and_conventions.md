TypeScript ESM codebase with relative imports using `.js` extensions. Use `backend/src/utils/logger.ts` rather than `console.*` in runtime code. Configuration belongs in `backend/src/config`. Read existing patterns before editing and prefer the smallest correct change. Protect live-trading safety first; validate the full execution path for trading changes. Route provider calls through the shared services in `backend/src/services/`, including `JupiterService`, `BirdeyeService`, `DexScreenerService`, and `MarketRouter`, so quota accounting, batching, caching, and auth stay centralized. Do not add raw provider fetches or docs that pretend Redis/BullMQ worker infrastructure already exists. Workers should reuse `backend/src/db/client.ts` rather than creating ad-hoc Prisma clients. Dashboard chrome reflects the active runtime scope from `use-dashboard-shell`, while page-level inspection filters live in `use-dashboard-filters`; do not blur those roles. Quota service totals are global provider budgets, even when endpoint drill-downs narrow by mode/config profile metadata. Avoid touching root-level files unless the task requires it.
